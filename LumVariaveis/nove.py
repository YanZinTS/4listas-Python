# Entrada da base e altura do triângulo
base = float(input("Digite a medida da base do triângulo: "))
altura = float(input("Digite a medida da altura do triângulo: "))

# Cálculo da área do triângulo
area = (base * altura) / 2

# Impressão do resultado
print(f"Área: {area}")